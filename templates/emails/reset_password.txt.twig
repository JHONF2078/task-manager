{# templates/emails/reset_password.txt.twig #}
Hola {{ user.name ?? user.email }},

Recibimos una solicitud para restablecer tu contraseña.

Enlace de restablecimiento (copia y pega si no funciona el botón del correo HTML):
{{ resetUrl }}

Token parcial: {{ token|slice(0,8) }}
Este enlace caduca en 60 minutos. Si no solicitaste el cambio, ignora este mensaje.

--
MiApp - Soporte
Enviado: {{ "now"|date("Y-m-d H:i") }}

